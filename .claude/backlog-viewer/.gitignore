node_modules/
dist/
.astro/
.vercel/
.DS_Store
*.log

# Regenerated by predev/prebuild/pretest from task-status-registry.json —
# never hand-edited, never a sync source. Tracking it made caw upgrade ship a
# frozen snapshot to every project as if it were real hub source.
src/lib/status-map.generated.ts

# Local env (commit .env.example only — never .env)
.env
.env.local
.env.*.local
