# Generated by /caw-setup at 2026-09-08T00:00 (first run)
# Re-run /caw-setup --refresh to regenerate

detected_stack:
  frontend: next.js 16 (App Router) + react 19 + tailwind css v4
  map: maplibre-gl + react-map-gl (radar/weather tiles proxied via next api routes)
  database: supabase (postgres + realtime + storage), no ORM — plain @supabase/supabase-js
  ui: shadcn/ui (new-york style) + radix primitives + lucide-react
  lint_format: biome (no eslint config despite eslint/eslint-config-next present as deps)
  testing: none — no jest/vitest/playwright, no test files
  monorepo: none (pnpm-workspace.yaml present but single package, no apps/*)
  typescript: yes (tsconfig.json at root)

installed_skills:
  - stack: supabase
    skills: [supabase, supabase-postgres-best-practices]
    source: caw

  - stack: nextjs
    skills: [next-best-practices, next-cache-components, nextjs-feature]
    source: caw

  - stack: react
    skills: [react-component-testing, vercel-composition-patterns, vercel-react-best-practices]
    source: caw

  - stack: shadcn/tailwind
    skills: [shadcn, shadcn-table-layout, tailwind-design-system]
    source: caw

  - stack: typescript
    skills: [typescript-advanced-types]
    source: caw

defaults:
  workflow:
    - to-prd
    - webapp-testing
    - javascript-testing-patterns
    - react-component-testing
    - test-driven-development
    - verification-before-completion
    - code-review-excellence
    - performance
    - accessibility
    - refactor
    - improve-codebase-architecture
    - systematic-debugging
    - find-skills
    - validate-skills
  product:
    - prd-development
    - user-story
    - user-story-splitting
    - prioritization-advisor
    - business-analyst
    - create-specification
    - roadmap-planning
  cross_cutting:
    - typescript-advanced-types
    - github
    - github-actions
  caw_owned:
    - api-contract
    - error-handling-patterns
    - nextjs-feature
    - adversarial-test-design
    - context-engineering
    - doubt-check
    - observability
    - performance-optimization
    - security-hardening
    - feedback-traceability
    - commit-conventions
    - package-manager
    - source-driven
    - runtime-smoke-test

skipped:
  - skill: turf-geospatial-helper
    reason: "No caw-curated or clearly-trusted hub skill for @turf/turf; niche geometry lib, low value vs. reading turf docs directly. Not searched further."

pending_hub_approval:
  - skill: maplibre-tile-sources
    repo: maplibre/maplibre-agent-skills
    installs: "670"
    reason: >
      Project's core map rendering (maplibre-gl + react-map-gl) plus 3 custom
      tile-proxy routes (radar, owm, isobands-derived risk layer) have no
      caw-curated skill covering MapLibre source/layer wiring. Official
      maplibre org, closest-installs match to raster tile source patterns
      this app actually uses. Awaiting user confirmation before
      `npx skills add maplibre/maplibre-agent-skills -s maplibre-tile-sources -y`.
  - skill: maplibre-cartography
    repo: maplibre/maplibre-agent-skills
    installs: "107"
    reason: >
      Styling/paint-property guidance for MapLibre layers (risk overlay,
      radar layer styling). Secondary/optional pick, lower installs than
      maplibre-tile-sources — only add if user wants deeper styling coverage.
