# Closed Tasks — {{PROJECT_NAME}}

Index of tasks that ended without shipping: superseded, dropped, deferred, or merged into another
task. One line per task so `/caw-plan` can check a proposed id or scope against work that was
already decided. Task folders stay under `tasks/` untouched; only `overview.yaml`'s `status:` moves
to `closed` with a comment pointing here.

| Task | Closed | Reason | Superseded by / note |
|---|---|---|---|
