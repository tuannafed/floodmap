# Harness Backlog — Misc Detail — {{PROJECT_NAME}}

Full write-ups for harness friction that is **not tied to one task** (spans several tasks, or was
hit outside any task). Friction tied to a task goes in `tasks/<id>/harness.md` instead. Either way,
the one-line index row lives in `harness-backlog.md` and links here by `HB-NNN`.

Use the detail template in `harness-backlog.md` (Discovered while / Pain / Suggested improvement / Risk / Status). One `### HB-NNN <title>` section per item; the index row lives in `harness-backlog.md`.
